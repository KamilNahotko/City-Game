const styles = (theme) => ({
  formSection: {
    marginTop: theme.spacing(4),
    display: "flex",
    flexDirection: "column",
    alignItems: "center",
  },
  comment: {
    width: "100%",
    marginTop: theme.spacing(2),
  },
  button: {
    width: "200px",
    marginTop: theme.spacing(4),
  },
});

export default styles;
