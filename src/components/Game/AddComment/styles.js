const styles = (theme) => ({
  formSection: {
    marginTop: theme.spacing(4),
    display: "flex",
    flexDirection: "column",
  },
  comment: {
    width: "100%",
  },
  styledButton: {
    marginTop: theme.spacing(4),
  },
});

export default styles;
