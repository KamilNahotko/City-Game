const styles = (theme) => ({
  styledTableContainer: {
    width: "60%",
    margin: "0 auto",
  },
  styledTitle: {
    textAlign: "center",
    marginBottom: theme.spacing(2),
  },
});

export default styles;
