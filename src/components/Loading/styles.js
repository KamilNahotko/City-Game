const styles = (theme) => ({
  root: {
    display: "flex",
    height: "70vh",
    justifyContent: "center",
    alignItems: "center",
  },
  typography: {
    margin: theme.spacing(2),
  },
});

export default styles;
